name: KSS Nightly Sync

on:
  schedule:
    - cron: '0 8 * * *'  # Runs daily at 8:00 AM UTC (1:00 AM PT)
  workflow_dispatch:       # Allows manual trigger from Actions tab

# Only one sync at a time — queue rather than run overlapping copies, which
# was racing on the final push (checkout diverging from main mid-run).
concurrency:
  group: kss-nightly-sync
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run KSS sync
        env:
          KSS_API_KEY: ${{ secrets.KSS_API_KEY }}
          KSS_BASE_URL: ${{ secrets.KSS_BASE_URL }}
        run: python scripts/kss_sync.py

      - name: Run transform
        run: python scripts/kss_transform.py

      - name: Score menu health
        run: python scripts/menu_health_score.py

      - name: Commit and push data files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/*.json config/rep_assignments.json
          git diff --staged --quiet || git commit -m "Nightly KSS data sync"
          git pull --rebase origin main
          git push
